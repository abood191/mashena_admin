# Production: direct requests to the API.
# Both mashena.wasta-jobs.com and api-mashena.wasta-jobs.com share the same
# eTLD+1 (wasta-jobs.com), so they are same-site.
# SameSite=Lax cookies are sent automatically — no proxy needed.
VITE_API_URL=https://api-mashena.wasta-jobs.com
