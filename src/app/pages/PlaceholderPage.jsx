export default function PlaceholderPage({ title }) {
  return (
    <div>
      <h1 className="text-xl font-semibold">{title}</h1>
      <p className="mt-2 text-white/60">
        Placeholder — لسه ما قررنا تفاصيل الصفحة.
      </p>
    </div>
  );
}
